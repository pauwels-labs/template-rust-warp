apiVersion: v1
kind: Service
metadata:
  name: {{ include "pauwels-labs.fullname" . }}
  labels:
    {{- include "pauwels-labs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pauwels-labs.selectorLabels" . | nindent 4 }}
